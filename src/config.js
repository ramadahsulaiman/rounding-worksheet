// Default to local API for dev, change when deployed
// export const API_BASE = import.meta.env.VITE_API_BASE || "http://localhost:4000";

// Use relative path; Netlify will route to the function
export const API_BASE = "";
